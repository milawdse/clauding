"""Reference solutions, one module per notebook (`m01.py` … `m12.py`).

Two audiences:

* **You**, once you've had a real go at an exercise. Each file is plain
  Python you can read, run, and compare against your own attempt.
* **`tools/verify.py`**, which injects these into a fresh run of each
  notebook and asserts every checker passes — so an exercise can never ship
  broken or unsolvable.

Because they're exec'd into the notebook's namespace, solutions may freely
use helpers the lecture defined above the exercise.
"""
